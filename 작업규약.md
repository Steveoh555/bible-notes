# 성경연구노트 — 사이트 작업 규약

> 새 자료 페이지를 만들 때 지켜야 할 것들을 모아 둔 문서입니다.
> 색·컴포넌트·차트 팔레트가 여기 정리되어 있어, 페이지마다 모양이 달라지지 않습니다.

성경 배경 연구 자료를 **손으로 설계한 HTML 페이지 그대로** 모아 공개하는 정적 사이트입니다.
서버도 데이터베이스도 없습니다. 파일이 곧 사이트입니다.

> **마크다운을 자동 변환하는 사이트가 아닙니다.** 각 자료는 표·도표·그림을 손으로 설계한
> 독립된 HTML 페이지입니다. Quartz 같은 SSG는 검토했다가 이 요구에 맞지 않아 폐기했습니다.

## 구조

```
site/
├── index.html          홈 — 자동 생성. 절대 직접 고치지 말 것
├── about.html          소개
├── studies/*.html      ← 자료 페이지
├── assets/site.css     공통 디자인 시스템 (색·글꼴은 :root 토큰만 고치면 전체 반영)
├── assets/site.js      테마 전환(자동/밝게/어둡게) + 홈 검색
├── build.py            자동화
├── verify.py           점검
├── site.config.json    사이트 이름·주소·글쓴이
├── sitemap.xml         자동 생성   ┐ site_url 이 비어 있으면
└── feed.xml            자동 생성   ┘ 생성되지 않음
```

## 명령

```bash
python3 build.py new <슬러그> "<제목>"     새 자료 뼈대 생성
python3 build.py                          홈·sitemap·feed 재생성 + 공통 요소 주입
python3 build.py standalone <슬러그> [폴더] css/js를 안에 넣은 단일 파일 추출
python3 verify.py                         링크·태그·메타·토큰 점검
python3 -m http.server 8000               로컬 미리보기
```

## 자료 페이지 규약 — 반드시 지킬 것

### 1. `<head>` 안에 자기 정보를 적는다

홈 카드와 검색 결과가 여기서 나옵니다.

```html
<title>제목 — 성경연구노트</title>
<meta name="description" content="한 문장 요약. 검색 결과에 그대로 보입니다.">
<meta name="study:scripture" content="누가복음 2:1–7">
<meta name="study:tags" content="연대기, 로마사">
<meta name="study:date" content="2026-08-30">
<meta name="study:updated" content="2026-08-30">
<meta name="study:draft" content="true">   <!-- 있으면 목록에서 숨김 -->
```

### 2. 세 개의 표시자를 남긴다

`build.py`가 이 자리를 채웁니다. 지우면 헤더·푸터·SEO 태그가 안 들어갑니다.

```html
<!--#HEAD--><!--/#HEAD-->        (</head> 바로 위)
<!--#HEADER--><!--/#HEADER-->    (<body> 바로 아래)
<!--#FOOTER--><!--/#FOOTER-->    (</body> 바로 위)
```

### 3. 공통 CSS를 불러 쓴다

```html
<link rel="stylesheet" href="../assets/site.css">
<script src="../assets/site.js"></script>
```
페이지 고유 스타일만 `<style>`로 추가합니다.

## 디자인 시스템

**방향:** 비평본 인쇄물(critical edition). 종이 질감, 규칙선, 난외 표기, **단일 강조색**.
2026-08-30 taste-skill / redesign-skill 감사에 따라 전면 개편.

### 색 토큰 (라이트·다크 3상태 모두 정의)

| 토큰 | 쓰임 |
|---|---|
| `--accent` `--accent-soft` `--accent-ink` | **유일한 브랜드 강조색(청동 녹).** 링크·눈썹문구·현재페이지·호버 |
| `--judea` `--judea-soft` | `--accent` 의 별칭 (기존 페이지 호환) |
| `--rome` `--rome-soft` | **의미색 — 표·도표 안에서만.** 로마 쪽(황제·총독) |
| `--temple` `--temple-soft` | **의미색 — 표·도표 안에서만.** 성전·성경 인용 |
| `--ground` `--surface` `--surface-2` | 바탕 3단계 |
| `--line` `--line-soft` | 규칙선 2단계 |
| `--ink` `--ink-2` `--muted` | 글자 3단계 |
| `--r-lg` `--r-md` `--r-sm` | 모서리 반경 **3단계** — 바깥 컨테이너는 크게, 안쪽은 작게 |

> **강조색은 하나다.** 장식으로 `--rome`/`--temple` 을 쓰지 말 것. 그 둘은
> 로마/성전이라는 **내용상 구분**이 있을 때만 표·칩·도표 안에서 쓴다.

### 규칙

- **대문자 변환(`text-transform:uppercase`) 금지.** 눈썹 문구는 소문자 + 규칙선(`.eyebrow::after`).
- **카드 그리드 금지.** 홈은 `.index-list > li > a.entry` 색인 목록
  (좌: 성경 본문 참조 모노스페이스 / 우: 제목·설명·날짜). 비대칭 2단.
- 종이 질감은 `body::after` 의 feTurbulence 노이즈. 건드리지 말 것.
- 모든 상호작용 요소에 hover **와** `:active` 를 준다. 전환 180~220ms, `--ease`.
- 숫자가 줄 맞춰야 하는 곳은 `font-variant-numeric:tabular-nums`.
- 본문 폭 65ch 이하, 긴 문단에 `text-wrap:pretty`, 제목에 `text-wrap:balance`.
- `color-mix()` 를 쓰면 **바로 앞줄에 단색 폴백**을 둔다.

### 글꼴

`--display` Hahmlet → Gowun Batang → Nanum Myeongjo → Georgia
`--body` IBM Plex Sans KR → Apple SD Gothic Neo → Malgun Gothic
`--mono` IBM Plex Mono (장절 표기·수치 전용)

> 구글 폰트는 이 작업 환경에서 차단돼 **렌더 확인이 불가능하다.** 폰트를 바꾸면
> 반드시 폴백을 겹쳐 두고 사용자에게 화면 확인을 부탁할 것.

### 공통 컴포넌트

`.ladder>.rung+.arrow` · `.scroller>table` · `.chip.rome/.judea/.temple` · `.tip` ·
`.hinge` · `.mapcard` · `.cards>.hcard` · `.glossary>.term` · `.prose` · `.stamp` ·
`.eyebrow` · `.tiles>.tile` · `.two>.view` · `blockquote.bible` · `blockquote.src` ·
`.index-list>.entry` (홈 전용)

### 자동으로 챙겨지는 것 (build.py)

파비콘 · 애플 터치 아이콘 · 테마색 · `og:image` · 건너뛰기 링크 · `id="main"` ·
네비게이션 `aria-current` · `404.html` · `sitemap.xml` · `feed.xml` · `인용현황.md`

### 차트

사이트 강조색은 채도가 낮아 dataviz 검증(chroma floor)을 통과하지 못한다.
차트 전용 검증 통과 팔레트 — **라이트 `#008F80`+`#B2591F`, 다크 `#22A88F`+`#C97E33`**.
페이지 `<style>` 에서 `--s1/--s2` 를 3상태 모두 정의. 범례·직접라벨·`<details>` 수치표·툴팁 포함.

## 성경 본문 인용

**개역개정을 쓰되 한 번에 1~2절만.** 한 장 전체나 긴 단락은 싣지 않습니다.
인용마다 아래 마크업을 쓰면 `build.py` 가 `인용현황.md` 에 자동 집계합니다.

```html
<blockquote class="bible" data-ref="누가복음 2:4-5" data-version="개역개정">
  <p>본문…</p>
  <cite>누가복음 2:4–5 · 개역개정</cite>
</blockquote>
```

성경이 아닌 고전 인용(요세푸스·외경·교부)은 `class="src"` 를 씁니다 — 저작권 집계에서 빠집니다.

```html
<blockquote class="src"><p>…</p><cite>요세푸스 『유대 고대사』 18.1.1</cite></blockquote>
```

`verify.py` 가 한 인용 4절 초과와 한 페이지 같은 장 8절 초과를 경고합니다.
자세한 방침은 `../docs/공개-계획.md`.

## 이미지 — 사료 도판과 AI 삽화

이미지는 두 종류뿐이고, 둘을 **섞어 보이게 하지 않는다.**

| 분류 | 무엇 | 어디에 | 허용 라이선스 |
|---|---|---|---|
| `사료` | 실제 유물 사진·고지도·고문서 스캔 | 본문 속 도판 | **PD / CC0 만** |
| `삽화` | AI 로 만든 분위기 그림 | 제목 위 히어로 밴드 | — |

### 원장이 먼저다

모든 이미지는 `assets/img/출처.json` 에 먼저 등록한다. 등록하지 않으면 `verify.py` 가
배포를 막는다. `사료` 항목에는 제목·라이선스·원본페이지가 반드시 있어야 한다.

### 본문에 넣는 법

표시자 한 줄이면 된다. 나머지는 `build.py` 가 채운다.

```html
<!--#PLATE:coin-augustus.jpg|sm--><!--/#PLATE-->
```

| 옵션 | 폭 | 쓰임 |
|---|---|---|
| `sm` | 440px | 주화, 인장처럼 작은 유물 |
| `tall` | 620px | 세로로 긴 고지도, 문서 |
| 없음 | 본문 폭 | 가로로 넓은 자료 |

`build.py` 가 원장에서 제목·연도·설명·소장처·라이선스·원본 링크를 읽어
그림 아래 출처 문단을 만들고, 이미지를 원본 페이지로 링크한다.

### 캡션 규칙

| 분류 | 캡션 | 위치 |
|---|---|---|
| `사료` | 제목·연도 / 설명 / 소장처·라이선스·원본 링크 | 이미지 아래, 왼쪽 강조 막대 |
| `삽화` | **내용을 돕기 위한 AI 생성 삽화입니다.** (고정) | 이미지 **하단 오른쪽 끝**, 작게 |

AI 삽화 캡션 문구는 `build.py` 의 `AI_NOTE` 한 곳에서만 관리한다. 페이지마다 손으로 적지 않는다.
사료 도판과 달리 출처 막대를 두지 않아, 눈으로도 둘이 구별된다.

### 왜 이렇게까지 하나

이 사이트의 값어치는 **1차 자료로 확인했다**는 데 있다. 출처 없는 유물 사진이나
사료처럼 보이는 AI 그림이 한 장이라도 섞이면 나머지 검증까지 함께 의심받는다.
`verify.py` 의 [7] 검사는 그 사고를 막으라고 있는 것이다.

## 배포

git 저장소이며 GitHub Pages로 배포합니다(Cloudflare는 이 환경에서 막혀 있음).
`./publish.sh "커밋 메시지"` 한 줄로 빌드→점검→커밋→푸시가 끝납니다.
`site.config.json`의 `site_url`이 비어 있으면 sitemap·feed·canonical이 생성되지 않습니다.
