# 성경연구노트 — 사이트 작업 규약

> 새 자료 페이지를 만들 때 지켜야 할 것들을 모아 둔 문서입니다.
> 색·컴포넌트·차트 팔레트가 여기 정리되어 있어, 페이지마다 모양이 달라지지 않습니다.

성경 배경 연구 자료를 **손으로 설계한 HTML 페이지 그대로** 모아 공개하는 정적 사이트입니다.
서버도 데이터베이스도 없습니다. 파일이 곧 사이트입니다.

> **마크다운을 자동 변환하는 사이트가 아닙니다.** 각 자료는 표·도표·그림을 손으로 설계한
> 독립된 HTML 페이지입니다. Quartz 같은 SSG는 검토했다가 이 요구에 맞지 않아 폐기했습니다.

## 구조

```
site/
├── index.html          홈 — 자동 생성. 절대 직접 고치지 말 것
├── about.html          소개
├── studies/*.html      ← 자료 페이지
├── assets/site.css     공통 디자인 시스템 (색·글꼴은 :root 토큰만 고치면 전체 반영)
├── assets/site.js      테마 전환(자동/밝게/어둡게) + 홈 검색·차례·「더 보기」
├── build.py            자동화
├── verify.py           점검
├── site.config.json    사이트 이름·주소·글쓴이
├── sitemap.xml         자동 생성   ┐ site_url 이 비어 있으면
└── feed.xml            자동 생성   ┘ 생성되지 않음
```

## 명령

```bash
python3 build.py new <슬러그> "<제목>"     새 자료 뼈대 생성
python3 build.py                          홈·sitemap·feed 재생성 + 공통 요소 주입
python3 build.py standalone <슬러그> [폴더] css/js를 안에 넣은 단일 파일 추출
python3 verify.py                         링크·태그·메타·토큰 점검
python3 -m http.server 8000               로컬 미리보기
```

## 자료 페이지 규약 — 반드시 지킬 것

### 1. `<head>` 안에 자기 정보를 적는다

홈 카드와 검색 결과가 여기서 나옵니다.

```html
<title>제목 — 성경연구노트</title>
<meta name="description" content="한 문장 요약. 검색 결과에 그대로 보입니다.">
<meta name="study:scripture" content="누가복음 2:1–7">
<meta name="study:tags" content="연대기, 로마사">
<meta name="study:date" content="2026-08-30">
<meta name="study:updated" content="2026-08-30">
<meta name="study:draft" content="true">   <!-- 있으면 목록에서 숨김 -->
```

### 2. 세 개의 표시자를 남긴다

`build.py`가 이 자리를 채웁니다. 지우면 헤더·푸터·SEO 태그가 안 들어갑니다.

```html
<!--#HEAD--><!--/#HEAD-->        (</head> 바로 위)
<!--#HEADER--><!--/#HEADER-->    (<body> 바로 아래)
<!--#FOOTER--><!--/#FOOTER-->    (</body> 바로 위)
```

### 3. 공통 CSS를 불러 쓴다

```html
<link rel="stylesheet" href="../assets/site.css">
<script src="../assets/site.js"></script>
```
페이지 고유 스타일만 `<style>`로 추가합니다.

## 문체 — 반드시 지킬 것 (2026-09-01 확정)

**기준 페이지는 `studies/bethlehem-rulers.html` 입니다.** 새 자료를 쓸 때 그 페이지를 먼저 열어
말투를 눈으로 맞춘 뒤 시작하세요.

### 원칙 한 줄

> **내용을 쉽게 만들지 않는다. 어려운 내용을 쉬운 말투로 안내한다.**

기준 페이지도 민족장(이더나크)·분봉왕(테트라크) 같은 낯선 직함을 그대로 쓰고,
요세푸스를 권·장 단위로 인용하고, "±1년 오차 여지가 있습니다"라고 사료 신뢰도까지 밝힙니다.
쉬운 것은 **말투**이지 내용이 아닙니다.

**표·전수 집계·판정표·역본 대조·원문 인용은 밀도를 그대로 지킵니다.**
바뀌는 것은 그것들을 소개하고 해석하는 문장뿐입니다.

### 다섯 장치

1. **해요체** — 설명하는 본문 전부.
2. **질문형 소제목** — 독자가 품을 의문을 그대로 `<h2>`에 올립니다. "헤롯은 정말 왕이었을까요?"
3. **결론 먼저, 근거 나중** — 문단의 첫 문장이 답입니다. "아니었어요." 하고 시작한 뒤 근거를 폅니다.
4. **어려운 낱말은 지우지 말고 그 자리에서 풉니다.** 용어가 다섯 개를 넘으면
   페이지 끝에 `.glossary > .term` 으로 **「어려운 말 풀이」** 절을 답니다.
5. **이중 레지스터** — 「근거 자료」 절만 **합니다체**로 바꿉니다.
   설명하는 목소리와 책임지는 목소리를 구분하는 장치이고, 이것이 페이지의 무게를 지탱합니다.

### 어디에 무엇을 쓰나

| 자리 | 문체 | 예 |
|---|---|---|
| `<h1>` `<h2>` `<h3>` | 명사구 또는 **질문형** | "독수리는 멋진 새였을까요?" |
| `.lede` `.sub` `.prose p` `.tip p` | **해요체** | "…본문 분포와 맞지 않아요." |
| 표의 짧은 라벨 칸 | 종결어미 없는 **명사구** | "수리아 총독" · "3~4일" |
| 표 안 설명 문장(`.note` `.alt`) | **해요체** | "군단 3~4개를 거느린 동방 최고 자리였어요." |
| 판정표의 **항목** 칸 | **하다체 허용** | "…주제로 삼는다" (판정 대상이 되는 명제이므로) |
| 판정표의 **판정** 칸 | 낱말 그대로 | 확인 / 반증 / 미결 / 가설 |
| `.chart-head p` · `figcaption` | **해요체** | "막대에 손을 올리면 구절이 보여요." |
| 도판 캡션 (`assets/img/출처.json`) | **하다체** | "…예수님이 태어나시기 약 30년 전에 만들어졌다." |
| **「근거 자료」 절** | **합니다체** | "…±1년 정도 오차 여지가 있습니다." |
| 푸터 | 합니다체 (build.py 자동) | — |

### 문장 규칙

- **한 문장에 한 가지.** 두 절 이상 이어지면 끊습니다.
- **숫자** — 본문 산문에서는 한글로 풀어도 좋습니다("스물여섯 자리"). 표·타일·차트의 수치는 아라비아 숫자 그대로.
- **원어** — 뜻을 먼저 말하고 원문을 뒤에 붙입니다. "지치다(יָעֵף)" 순서이지 그 반대가 아닙니다.
- **장절**은 문장 끝 괄호로 내립니다. "…맨 첫 줄이에요(레위기 11:13)."
- **높임** — 예수님·요셉·마리아는 높입니다. 고대 인물(헤롯·요세푸스·느부갓네살)은 평칭.
- **금지** — 설교조 마무리, 감탄사, "~것이다" 단정, 독자를 가르치는 어투("~해야 합니다"),
  감정을 신체 반응으로 대체하는 묘사, 감각어 남발.
- **모르는 것은 모른다고 씁니다.** "미결이에요", "본문은 말하지 않아요".

### 바꿔 쓰는 낱말 — 계속 갱신하는 목록 (2026-09-17 시작)

사용자가 읽다가 "AI 특유의 어휘"로 걸러낸 낱말을 여기에 **누적**한다.
새 페이지를 쓸 때 이 표를 먼저 훑고, 새로 지적받은 낱말이 나오면 **그 자리에서 한 줄 더 적는다.**
고치기만 하고 적지 않으면 다음 페이지에서 똑같이 반복된다.

| 쓰지 않는다 | 대신 쓴다 | 왜 |
|---|---|---|
| **자리** (횟수·대목의 뜻) | **번** · **곳** · **대목** | "나머지 두 자리는" → "나머지 두 번은". 사람이 앉거나 서는 곳이 아닌데 `자리`를 쓰면 번역문처럼 읽힌다 |
| **타일** | **숫자 칸** | 화면 부품 이름을 본문에 노출하지 않는다. "두 번째 타일이" → "가운데 숫자 칸이" |
| **정식**(定式) | **선언** · **굳어진 표현** · **틀** | "저주의 정식" → "저주 선언", "낮춰 부르는 정식" → "낮춰 부르는 굳어진 표현" |
| **무너짐** (판정 칸) | **반증** | 뜻은 맞지만 번역어투로 읽힌다. 판정 낱말은 **확인 / 반증 / 미결 / 가설** 넷으로 통일한다 |

**`자리`를 써도 되는 경우** — 사용자가 승인한 용법이다.

- 사람이 실제로 앉거나 서는 곳: "그 두 사람이 서 있던 자리", "아멘의 자리에 서 있다"
- 본문에서 **물음이 던져지는 대목**을 가리킬 때: "앞의 둘은 …를 묻는 자리이고, 뒤의 둘은 …를 묻는 자리예요"
- 그 일이 일어나는 **터**라는 신학적 비유가 논지 자체일 때: "마리아는 그 일이 일어날 자리로 들어가요"

또 **`자리`가 관용구나 직위를 뜻할 때도 그대로 쓴다.**

- 직위·왕위(실제로 앉는 좌석에서 온 말): "다스리는 자리는 아니었어요", "헤롯의 자리를 물려주지 않고", "동방 최고 자리"
- 굳은 관용구: "자리 잡다", "옆자리에 앉았다"
- 자릿수: "여덟 자리 분석 부호의 '법' 자리"

경계가 애매하면 **`곳`으로 바꿔 읽어 보고 뜻이 그대로면 `곳`을 쓴다.**

**2026-09-17 기존 공개 페이지 일괄 정리 완료.** 독수리 7건 · 테레오 12건 · 나사렛 4건 · 베드로 4건 ·
사자와 어린양 5건 · 마리아 18건을 교체했다. 통치자들 페이지는 여덟 건 모두 직위 뜻이라 그대로 두었다.

### 색인에 붙이는 분량·성격 표시 (2026-09-17 확정)

**난도를 별점으로 매기지 않는다.** 사용자가 별 1~3개를 제안했고, 아래 이유로 다른 방식을 택했다.

1. **별은 평점으로 읽힌다.** ★★★ 이 "어렵다"가 아니라 "좋다"로 읽히고, ★1 은 "부실한 글"로 보인다.
2. **어렵다고 적으면 어려운 글을 피한다.** 이 사이트의 값어치는 밀도에 있는데 그 밀도를 경고로 만들면 손해다.
3. **한 페이지 안에서도 난도가 균일하지 않다.** 앞 절은 쉽고 판정표는 어렵다. 숫자 하나로 못 담는다.

대신 **객관적으로 셀 수 있는 분량**과 **페이지가 스스로 밝히는 성격** 두 칸을 색인 행에 적는다.

| 칸 | 어디서 오나 | 표시 |
|---|---|---|
| **읽는 시간** | `build.py` 의 `reading_minutes()` 가 태그를 벗긴 글자 수를 세어 계산 (`CPM = 500`, 최소 3분) | `.entry-read` — 글자색만 |
| **성격** | 페이지의 `<meta name="study:kind">` | `.entry-kind` — 강조색을 옅게 깐 칩 |

성격은 **셋 중 하나**로 쓴다. 어느 쪽이 더 훌륭하다는 뜻이 없어야 한다.

- **원문 파고들기** — 헬라어·히브리어 전수 집계와 사본 대조가 본문의 중심인 자료
- **배경 읽기** — 역사·지리·제도 배경이 중심인 자료
- **본문 따라가기** — 한 단락의 흐름을 절 단위로 따라가는 자료

- 시간은 자동 계산이므로 **새 자료를 올릴 때 손으로 적지 않는다.** 적어야 하는 것은 `study:kind` 한 줄뿐이다.
- 성격 표시가 붙으면 색인 행이 빽빽해지므로 `build.py` 가 태그를 **셋에서 둘로** 줄인다.
- `study:kind` 는 홈 검색어에도 들어간다 — "원문"으로 검색하면 해당 자료가 걸린다.
- `verify.py` **[10] 색인 표시** 가 빠진 `study:kind` 를 알려 준다. **권고라 배포를 막지 않는다.**

### 원문 표에는 사역을 붙인다 (2026-09-17 추가)

헬라어·히브리어 원문을 표에 넣을 때는 **그 아래에 한글 사역을 함께 적는다.**
원문만 있으면 읽는 사람이 건너뛰게 된다. 사역은 `<span class="note">` 로 달고,
칸 머리는 「첫머리 원문 · 사역」처럼 사역이 있다는 것을 밝힌다.
`td` 에 `.gk` 가 걸려 있으면 사역까지 모노 글꼴이 되므로 `.gk .note{font-family:var(--body)}` 를 페이지 `<style>` 에 둔다.
사역의 말투는 **하다체**("마리아가 말하였다")로 적는다 — `verify.py` [8] 문체 검사가 하다체는 세지 않는다.


### 고쳐쓸 때 흔한 실패

**어미만 치환하면 안 됩니다.** "…맞지 않습니다" → "…맞지 않아요" 로 바꾸는 것만으로는
말투는 친근한데 문장 구조가 그대로여서 기준 페이지와 다른 사람 글로 읽힙니다.
문장을 끊고, 결론을 앞으로 빼고, 관형절을 풀어야 같은 목소리가 됩니다.

### 줄바꿈 — 의존명사는 줄 첫머리에 두지 않는다 (2026-09-01 추가)

한국어 줄바꿈은 세 겹으로 지켜진다. **두 번째 겹은 자동으로 안 되므로 손으로 넣어야 한다.**

| 겹 | 지키는 것 | 어떻게 |
|---|---|---|
| 1. 어절 | 낱말 중간에서 안 끊기게 | `word-break:keep-all` (site.css, 자동) |
| 2. **구(의미 단위)** | **붙어야 할 두 어절이 안 떨어지게** | **`class="nb"` 로 직접 묶는다** |
| 3. 줄 균형 | 마지막 줄이 외톨이가 안 되게 | `text-wrap:balance`(제목) / `pretty`(본문), 자동 |

**3번이 2번을 모른다.** `balance` 는 줄 길이만 계산하므로, 하필 의미가 끊기는 자리가
길이상 가장 고른 자리면 거기서 자른다. 실제로 소개 페이지 제목이 이렇게 깨져 있었다.

```
확인한 것과 추론한      ← "것"은 혼자 못 서는 낱말인데 줄 첫머리에 떨어졌다
것을 구분해서 씁니다
```

묶어 주면 의미대로 갈라진다. **[무엇을] / [어떻게 한다].**

```html
<h1><span class="nb">확인한 것과</span> <span class="nb">추론한 것을</span> 구분해서 씁니다</h1>
```
```
확인한 것과 추론한 것을
구분해서 씁니다
```

**묶어야 하는 짝**

| 종류 | 예 |
|---|---|
| 관형어 + 의존명사 | 추론한 **것**, 갈 **수**, 태어나셨을 **때**, 아는 **바**, 그럴 **리**, 하는 **데** |
| 수 + 단위·의존명사 | 26 **회**, 143~164 **km**, 3 **일**, 열두 **명** |

**적용 범위:** 제목(`h1`~`h3`)과 짧은 표시 문구(`.lede` `.sub` `.stamp` `figcaption`
`caption` `summary` `dt`)까지만. **본문 산문에는 걸지 않는다** — 남발하면 오른쪽 여백이
들쭉날쭉해지고, 본문은 이미 잘 흐른다.

**방식은 `class="nb"`(= `white-space:nowrap`)를 쓴다. `&nbsp;` 는 쓰지 않는다.**
붙임 공백은 글자 자체가 일반 공백이 아니게 되어, 방문자가 페이지에서 낱말을 찾을 때
안 걸릴 수 있다. `nb` 는 텍스트를 그대로 두고 줄바꿈만 막는다.
(`<span class="nb">` 가 기본이고, 이미 `<b>` 같은 태그가 있으면 `class="nb"` 만 얹어도 된다.)

**점검:** `verify.py` **[9] 줄바꿈** 이 묶이지 않은 짝을 찾아 알려 준다.
**권고일 뿐 배포를 막지 않는다** — 실제로 그 자리에서 줄이 끊기는지는 폭에 따라 달라지기 때문이다.
브라우저에서 확인하려면 [[browser-preview]] 의 줄 측정 코드를 쓴다.

### 자가 점검

`verify.py` **[8] 문체** 가 페이지별 어미를 세어 **해요체와 합니다체가 섞이면 경고**합니다.
(줄바꿈은 **[9] 줄바꿈** 이 따로 본다. 이쪽은 권고라 배포를 막지 않는다.)

- 검사 대상은 **`studies/` 안의 자료 페이지뿐**입니다. 홈·404·소개(`about.html`)는 제외됩니다.
- 「근거 자료」·「참고 자료」·「참고 문헌」 절, 푸터, 히어로 밴드의 AI 삽화 고정 문구도 제외됩니다.
- 하다체(`…이다`)는 세지 않습니다. 판정표 항목과 도판 캡션이 하다체이기 때문입니다.

## 디자인 시스템

**방향:** 비평본 인쇄물(critical edition). 종이 질감, 규칙선, 난외 표기, **단일 강조색**.
2026-08-30 taste-skill / redesign-skill 감사에 따라 전면 개편.

### 색 토큰 (라이트·다크 3상태 모두 정의)

| 토큰 | 쓰임 |
|---|---|
| `--accent` `--accent-soft` `--accent-ink` | **유일한 브랜드 강조색(청동 녹).** 링크·눈썹문구·현재페이지·호버 |
| `--judea` `--judea-soft` | `--accent` 의 별칭 (기존 페이지 호환) |
| `--rome` `--rome-soft` | **의미색 — 표·도표 안에서만.** 로마 쪽(황제·총독) |
| `--temple` `--temple-soft` | **의미색 — 표·도표 안에서만.** 성전·성경 인용 |
| `--ground` `--surface` `--surface-2` | 바탕 3단계 |
| `--line` `--line-soft` | 규칙선 2단계 |
| `--ink` `--ink-2` `--muted` | 글자 3단계 |
| `--r-lg` `--r-md` `--r-sm` | 모서리 반경 **3단계** — 바깥 컨테이너는 크게, 안쪽은 작게 |

> **강조색은 하나다.** 장식으로 `--rome`/`--temple` 을 쓰지 말 것. 그 둘은
> 로마/성전이라는 **내용상 구분**이 있을 때만 표·칩·도표 안에서 쓴다.

### 규칙

- **대문자 변환(`text-transform:uppercase`) 금지.** 눈썹 문구는 소문자 + 규칙선(`.eyebrow::after`).
- **카드 그리드 금지.** 홈은 `.index-list > li > a.entry` 색인 목록
  (좌: 성경 본문 참조 모노스페이스 / 우: 제목·설명·날짜). 비대칭 2단.
- **게시판식 페이지네이션(1·2·3) 금지.** 이유는 아래 「홈 목록이 길어지면」 참조.
- 종이 질감은 `body::after` 의 feTurbulence 노이즈. 건드리지 말 것.
- 모든 상호작용 요소에 hover **와** `:active` 를 준다. 전환 180~220ms, `--ease`.
- 숫자가 줄 맞춰야 하는 곳은 `font-variant-numeric:tabular-nums`.
- 본문 폭 65ch 이하, 긴 문단에 `text-wrap:pretty`, 제목에 `text-wrap:balance`.
- `color-mix()` 를 쓰면 **바로 앞줄에 단색 폴백**을 둔다.
- **`.alt` 는 `span` 전용이다.** `td` 에 직접 걸면 `display:block` 때문에 칸이 행에서 떨어져
  **배경색과 칸 경계가 어긋난다.** `<td><span class="alt">…</span></td>` 로 쓴다.
  (site.css 에 `td.alt{display:table-cell}` 안전망을 깔아 두었지만, 마크업부터 바르게 쓸 것.)
- **히브리어는 `.he` 클래스만 쓴다.** 모노 글꼴에는 히브리 글자가 없어 대체 글꼴로 떨어지면서
  모음 기호가 뭉쳐 읽을 수 없게 된다. `site.css` 의 `--hebrew` + 1.5em / 행간 1.7 이 정답이고,
  **페이지 `<style>` 에서 `.he` 를 다시 정의하지 말 것.** 히브리어 옆에는 한글 음역을 붙인다(네에만 · 헤에민).
- **히브리어 글꼴은 `Times New Roman` 으로 확정했다(2026-09-17).** 사용자가 화면을 보고 승인한 그 글꼴이다.
  Noto Serif Hebrew·David Libre·Frank Ruehl CLM 을 앞에 두었더니 **이 기기에 없어서 전부 일반 세리프로 떨어졌고**,
  실제로 그려진 것은 스택 맨 뒤의 Times New Roman 이었다. 그래서 순서를 뒤집어 맨 앞에 두었다.
  `--hebrew:"Times New Roman",Tinos,"Liberation Serif","David Libre",serif`
  (Tinos·Liberation Serif 는 Times New Roman 과 자폭이 같은 리눅스용 대체 글꼴이다.)
  **확인 방법:** `document.fonts.check()` 는 없는 글꼴도 true 를 돌려주므로 믿을 수 없다.
  캔버스로 폭을 재서 비교한다 — 같은 히브리 낱말이 Times New Roman 37.1px, 일반 세리프 47.5px 로 갈렸다.

### 글꼴

`--display` Hahmlet → Gowun Batang → Nanum Myeongjo → Georgia
`--body` IBM Plex Sans KR → Apple SD Gothic Neo → Malgun Gothic
`--mono` IBM Plex Mono (장절 표기·수치 전용)

> 구글 폰트는 이 작업 환경에서 차단돼 **렌더 확인이 불가능하다.** 폰트를 바꾸면
> 반드시 폴백을 겹쳐 두고 사용자에게 화면 확인을 부탁할 것.

### 글자 크기 — 6단계 고정 (2026-09-05 확정)

`:root` 의 `--fs-*` / `--fw-*` 토큰만 쓴다. **새 크기를 만들지 말 것.** rem·clamp·px 를 직접 적지 않는다.

| 토큰 | 크기/굵기 | 쓰는 곳 |
|---|---|---|
| `display` | 44px / 700 | 페이지 제목 `h1` |
| `h1` | 31px / 700 | 절 제목 `h2`, 수치 타일 큰 숫자 `.tile .v` |
| `h2` | 25px / 700 | `.hinge h3`, 홈 색인 제목 `.entry-title`, `.view .days` |
| `h3` | 20px / 600 | 카드·차트 제목(`h3`, `.hname`), 브랜드 `.brand b` |
| `body` | 16px / 400 | 본문, 표, 리드, 메뉴 — 선언하지 않고 상속 |
| `caption` | 13px / 500 | 메타·출처·칩·눈썹·표 머리·차트 축·툴팁 |

- 행간도 토큰(`--lh-*`)으로 고정한다. display 1.14 · h1 1.26 · h2 1.3 · h3 1.32 · body 1.75 · caption 1.5.
- 줄 길이는 본문·설명 모두 `max-width:58ch` (한글 약 40자), 푸터만 60ch.
- SVG 안의 글자는 뷰박스 배율을 보정한 값을 쓴다. 차트 축·라벨 14, 지도 지명 16·지역명 20.
  모바일에서 12px 아래로 내려가면 안 된다(지도는 `min-width:340px` + 가로 스크롤).
- **고아줄**: 여러 줄이 될 수 있는 문단(본문·목록·설명·인용·도판 설명)은 마지막 두 어절을 `.nb` 로 묶는다.
  `keep-all` 때문에 `text-wrap:pretty` 가 한글 마지막 어절을 못 다룬다. 묶음은 15자 이내(좁은 카드는 12자),
  표 셀은 넘칠 수 있어 묶지 않는다. 홈 색인 설명은 `build.py` 의 `nb_last2()` 가 자동으로 묶는다.
- Gowun Batang 은 400·700 만 있어서 `--display` 글꼴에 600 을 주면 700 으로 그려진다.

### 홈 목록이 길어지면 (2026-09-16 확정)

편수가 늘어도 **주소는 `index.html` 하나로 유지한다.** 게시판처럼 2페이지·3페이지로
나누지 않는다. 이유 셋: ① 홈 검색은 페이지에 그려진 목록만 훑으므로 잘라내면 뒷 페이지가
검색에서 사라진다 ② 정적 사이트의 2페이지부터는 크롤러가 거의 들어가지 않아 오래된 자료가
검색 유입에서 묻힌다 ③ 독자는 "새 글 확인"이 아니라 "내가 읽는 본문의 배경 찾기"로 온다.

대신 셋을 쓴다.

| 장치 | 동작 | 사는 곳 |
|---|---|---|
| **「더 보기」** | 처음 **5편**만 보이고 버튼마다 5편씩 이어 붙인다 | `#moreRow`·`.btn-more` |
| **차례 전환** | 「최신순 / 성경순」. 성경순은 창세기→계시록 정경 차례 | `.sort-tabs`·`.sort-tab` |
| **검색** | 검색어가 있으면 **자르지 않고 전체에서 찾아** 모두 보여 준다 | `#q` |

- **`build.py` 의 `PAGE_SIZE` 와 `assets/site.js` 의 `PAGE` 는 반드시 같아야 한다**(둘 다 5).
  build.py 가 6번째부터 `hidden` 을 박아 두고, site.js 가 그 뒤를 맡는다.
- 정경 차례는 `build.py` 의 `bible_key()` 가 `study:scripture` 의 **첫 책 이름**을 찾아
  `data-order="42002004"`(책2·장3·절3 자리) 로 찍는다. 책을 못 찾으면 `99000000` 으로 맨 뒤.
  긴 이름부터 맞춰 보므로 예레미야애가·요한계시록이 예레미야·요한복음으로 잘못 잡히지 않는다.
- 자바스크립트가 꺼진 화면에서는 `<noscript>` 가 숨긴 항목을 모두 펴고 버튼·차례를 감춘다.
  **전체 목록은 언제나 HTML 소스 안에 다 들어 있다** — 검색엔진은 6번째 이후도 본다.
- **40~50편을 넘기면** 홈은 최신 5편 + 주제 입구만 두고 전체 목록을 `studies/index.html`
  전용 색인 페이지로 옮긴다. 그때까지는 이 구조로 버틴다.

### 공통 컴포넌트

`.ladder>.rung+.arrow` · `.scroller>table` · `.chip.rome/.judea/.temple` · `.tip` ·
`.hinge` · `.mapcard` · `.cards>.hcard` · `.glossary>.term` · `.prose` · `.stamp` ·
`.eyebrow` · `.tiles>.tile` · `.two>.view` · `blockquote.bible` · `blockquote.src` ·
`.index-list>.entry` (홈 전용)

### 자동으로 챙겨지는 것 (build.py)

파비콘 · 애플 터치 아이콘 · 테마색 · `og:image` · 건너뛰기 링크 · `id="main"` ·
네비게이션 `aria-current` · `404.html` · `sitemap.xml` · `feed.xml` · `인용현황.md`

### 차트

사이트 강조색은 채도가 낮아 dataviz 검증(chroma floor)을 통과하지 못한다.
차트 전용 검증 통과 팔레트 — **라이트 `#008F80`+`#B2591F`, 다크 `#22A88F`+`#C97E33`**.
페이지 `<style>` 에서 `--s1/--s2` 를 3상태 모두 정의. 범례·직접라벨·`<details>` 수치표·툴팁 포함.

## 성경 본문 인용

**개역개정을 쓰되 한 번에 1~2절만.** 한 장 전체나 긴 단락은 싣지 않습니다.
인용마다 아래 마크업을 쓰면 `build.py` 가 `인용현황.md` 에 자동 집계합니다.

```html
<blockquote class="bible" data-ref="누가복음 2:4-5" data-version="개역개정">
  <p>본문…</p>
  <cite>누가복음 2:4–5 · 개역개정</cite>
</blockquote>
```

성경이 아닌 고전 인용(요세푸스·외경·교부)은 `class="src"` 를 씁니다 — 저작권 집계에서 빠집니다.

```html
<blockquote class="src"><p>…</p><cite>요세푸스 『유대 고대사』 18.1.1</cite></blockquote>
```

`verify.py` 가 한 인용 4절 초과와 한 페이지 같은 장 8절 초과를 경고합니다.
자세한 방침은 `../docs/공개-계획.md`.

## 이미지 — 사료 도판과 AI 삽화

이미지는 두 종류뿐이고, 둘을 **섞어 보이게 하지 않는다.**

| 분류 | 무엇 | 어디에 | 허용 라이선스 |
|---|---|---|---|
| `사료` | 실제 유물 사진·고지도·고문서 스캔 | 본문 속 도판 | **PD / CC0 만** |
| `삽화` | AI 로 만든 분위기 그림 | 제목 위 히어로 밴드 | — |

### 원장이 먼저다

모든 이미지는 `assets/img/출처.json` 에 먼저 등록한다. 등록하지 않으면 `verify.py` 가
배포를 막는다. `사료` 항목에는 제목·라이선스·원본페이지가 반드시 있어야 한다.

### 본문에 넣는 법

표시자 한 줄이면 된다. 나머지는 `build.py` 가 채운다.

```html
<!--#PLATE:coin-augustus.jpg|sm--><!--/#PLATE-->
```

| 옵션 | 폭 | 쓰임 |
|---|---|---|
| `sm` | 440px | 주화, 인장처럼 작은 유물 |
| `tall` | 620px | 세로로 긴 고지도, 문서 |
| 없음 | 본문 폭 | 가로로 넓은 자료 |

`build.py` 가 원장에서 제목·연도·설명·소장처·라이선스·원본 링크를 읽어
그림 아래 출처 문단을 만들고, 이미지를 원본 페이지로 링크한다.

### 캡션 규칙

| 분류 | 캡션 | 위치 |
|---|---|---|
| `사료` | 제목·연도 / 설명 / 소장처·라이선스·원본 링크 | 이미지 아래, 왼쪽 강조 막대 |
| `삽화` | **내용을 돕기 위한 AI 생성 삽화입니다.** (고정) | 이미지 **하단 오른쪽 끝**, 작게 |

AI 삽화 캡션 문구는 `build.py` 의 `AI_NOTE` 한 곳에서만 관리한다. 페이지마다 손으로 적지 않는다.
**삽화 캡션은 한 문장으로 끝낸다.** 예전에는 뒤에 "실제 유물이나 사료가 아닙니다."가 한 문장 더
붙어 있었으나 2026-09-01 사용자 지시로 삭제했다. 앞으로도 두 번째 문장을 덧붙이지 않는다.
사료 도판과 달리 출처 막대를 두지 않아, 눈으로도 둘이 구별된다.

### AI 삽화의 모델 선택 (2026-09-16 확정)

한 사이트 안에서 화풍이 갈리지 않도록 **그림 종류로 모델을 나눈다.**

| 그림 종류 | 모델 | 이유 |
|---|---|---|
| 히어로 밴드처럼 **인물이 없거나 아주 작은 풍경** | `nano_banana_pro` (21:9) | 성근 붓질·낮은 채도라 기존 히어로와 한 벌이고, 실제 회화로 오해될 여지가 적다 |
| **인물의 자세·구도가 중요한 본문 도판** | `seedream_v5_lite` (16:9) + 아래 팔레트 문구 | "등을 돌려, 얼굴 없이" 같은 구도 지시를 정확히 따른다. 나노바나나는 이 지시를 어겼다 |

시드림은 그대로 두면 색이 밝고 따뜻해져(19세기 유화 느낌) 히어로와 어긋난다.
**프롬프트에 반드시 이 문구를 붙인다:**

> heavily desaturated, cool palette. Palette strictly limited to pale sage-grey sky, warm limestone tan,
> deep verdigris green, cool grey-green shadow, oxidised bronze accents; almost no saturation,
> no warm sunlight, no blue sky, no bright white robe, no bright silver highlights.
> … seen from directly behind so no face is visible. No faces, no halo, no text, no lettering, no numerals.

후보는 모델당 2장씩 뽑아 **얼굴이 보이지 않는지, 본문의 논지와 싸우지 않는지**를 보고 고른다.
본문 도판은 `<!--#PLATE:파일.jpg--><!--/#PLATE-->` 로 넣으면 `build.py` 가 `plate ai` 클래스와
고정 캡션을 붙이고, `verify.py` [8] 문체 검사는 그 캡션을 세지 않는다.

### 생성한 이미지를 폴더로 가져오는 법 (2026-09-17 확립 — 중요)

힉스필드 결과물은 `d8j0ntlcm91z4.cloudfront.net` 에 올라오는데 **클로드가 쓰는 두 셸(클라우드 컨테이너, 기기 셸)의 프록시가 이 호스트를 403으로 막는다.** 임시 파일 호스트(`0x0.st`·`transfer.sh` 등)도 전부 막혀 있고, 폴더에서 파일을 올려 읽는 기능은 이 폴더가 구글 드라이브 동기화 폴더라 항상 "file is hardlinked" 오류가 난다.

**되는 길은 하나 — 사용자의 크롬은 CDN 에 접근이 된다.**

1. 크롬 탭을 이미지 URL 로 이동시킨다.
2. 그 페이지에서 다운로드를 건다.
   ```js
   const a=document.createElement('a'); a.href=location.href;
   a.download='hero-XXX.png'; document.body.appendChild(a); a.click(); a.remove();
   ```
3. `Downloads` 폴더 접근을 요청한다(바로 승인된다).
4. 기기 셸에서 `$HOME/mnt/Downloads/<파일>` 을 `$HOME/work` 로 복사 → 리사이즈 → `assets/img/` 로 복사.

**함정 셋**

- **파일 하나당 탭 하나.** 같은 탭에서 두 번째 다운로드를 걸면 크롬이 "여러 파일 다운로드" 정책으로 조용히 막는다.
- **`Downloads` 는 `ls` 가 I/O 오류**를 낸다(항목이 너무 많다). 파일명을 직접 지정해 `cp` 하면 정상 동작한다.
- **연결 폴더 안에서는 삭제·덮어쓰기가 막힌다.** 임시 파일은 `mnt` 밖(`$HOME/work`)에 두고 최종본만 복사한다. 실수로 들어간 임시 파일은 `_preview/`(gitignore 됨)로 옮긴다.

**후보를 눈으로 고르는 법 (2026-09-17 추가).** 후보 4장을 다 내려받은 뒤 **작은 미리보기를 연결 폴더 안에 만들어 스테이징**한다.
`$HOME/work` 는 `mnt` 밖이라 `device_stage_files` 가 닿지 않으므로, `convert 원본 -resize 900x -quality 80 $HOME/mnt/Downloads/prev-*.jpg` 로
미리보기를 **Downloads 안에** 만든 다음 그 경로를 스테이징해 컨테이너에서 `Read` 로 본다. 7MB 원본을 그대로 올리지 않아도 된다.
(`Downloads` 폴더 접근은 `device_request_folder_access` 로 바로 승인되지만, **승인 창이 응답 없이 닫히는 일이 있다.** 그때는
대화로 한 번 알린 뒤 다시 호출한다.)

**브라우저 확인 함정 하나 더.** 크롬 확장의 `javascript_tool` 안에서 **`window.scrollTo` 와 `scrollIntoView` 가 먹지 않는다**(scrollY 가 0 에서 안 움직임).
스크롤은 `computer` 의 `scroll` 액션으로 하고, 화면 아래쪽 lazy-load 이미지는 굳이 스크롤하지 말고
페이지 문맥에서 `fetch` + `createImageBitmap` 으로 **status·바이트·실제 크기를 재는 쪽이 확실하다.**

**하지 말 것 — base64 로 쪼개 옮기기.** 출력이 잘리고, 긴 base64 는 옮기는 과정에서 **데이터가 깨진다.** 실제로 12,000자 조각이 11,087자로 도착한 적이 있다. 깨진 이미지를 사이트에 올릴 위험이 있으므로 시도하지 않는다.

**스크린샷 주의.** 크롬 확장의 화면 캡처가 스크롤 직후나 페이지에서 스크립트를 실행한 뒤 빈 화면을 돌려줄 때가 있다. 사이트 문제로 오해하지 말 것. 확실한 확인법은 `naturalWidth` 와 `figcaption` 을 읽는 것이고, 새 탭에서 다시 열면 캡처도 정상으로 돌아온다.

### 왜 이렇게까지 하나

이 사이트의 값어치는 **1차 자료로 확인했다**는 데 있다. 출처 없는 유물 사진이나
사료처럼 보이는 AI 그림이 한 장이라도 섞이면 나머지 검증까지 함께 의심받는다.
`verify.py` 의 [7] 검사는 그 사고를 막으라고 있는 것이다.

## 배포

git 저장소이며 GitHub Pages로 배포합니다(Cloudflare는 이 환경에서 막혀 있음).
`./publish.sh "커밋 메시지"` 한 줄로 빌드→점검→커밋→푸시가 끝납니다.
`site.config.json`의 `site_url`이 비어 있으면 sitemap·feed·canonical이 생성되지 않습니다.
